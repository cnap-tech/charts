apiVersion: v2
name: app
description: Generic Helm chart for deploying Docker containers
type: application
version: 1.2.0
appVersion: 'latest'
annotations:
  artifacthub.io/images: |
    - name: app
      image: nginx:latest
      whitelisted: true
